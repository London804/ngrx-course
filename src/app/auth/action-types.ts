// this file is used to group all our auth actions together
// we are exporting it as AuthActions so we can use it throughout the application that way.
import * as AuthActions from './auth.actions';
// the * allows you import everything
// the as AuthActions is an alias to everything


export { AuthActions };
